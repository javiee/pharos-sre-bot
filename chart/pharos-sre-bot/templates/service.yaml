apiVersion: v1
kind: Service
metadata:
  name: {{ include "pharos-sre-bot.fullname" . }}
  labels:
    {{- include "pharos-sre-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "pharos-sre-bot.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
